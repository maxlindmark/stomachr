% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/impute.R
\name{impute_size}
\alias{impute_size}
\title{Impute missing prey and predator sizes}
\usage{
impute_size(
  dat,
  which = c("both", "prey", "pred"),
  method = c("lw_params", "observations"),
  size = c("both", "weight", "length"),
  fill_if_no_size = TRUE
)
}
\arguments{
\item{dat}{Tibble from \code{\link[=add_taxonomy]{add_taxonomy()}}.}

\item{which}{One of \code{"both"} (default), \code{"prey"}, or \code{"pred"}.}

\item{method}{One of \code{"lw_params"} (default) or \code{"observations"}.}

\item{size}{One of \code{"both"} (default), \code{"weight"}, or \code{"length"}.}

\item{fill_if_no_size}{If \code{TRUE} (default), prey records with neither weight
nor length borrow a size from the same stomach, same predator-prey pair,
or global species mean, then apply L/W.}
}
\value{
\code{dat} with imputed size columns and \code{prey_lw_source} /
\code{pred_lw_source} provenance columns. Internal L/W parameter columns are
dropped.
}
\description{
Estimates missing prey weight and/or length via L/W parameters or observed
means, and estimates missing predator weight from length. Calls the internal
\code{\link[=add_lw]{add_lw()}} helper to attach L/W parameters before imputation.
}
