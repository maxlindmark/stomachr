% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/join.R
\name{join_stomach_data}
\alias{join_stomach_data}
\title{Read and join the four ICES stomach content CSVs}
\usage{
join_stomach_data(path, impute_coords = TRUE)
}
\arguments{
\item{path}{Path to the directory containing the four ICES CSV files.}

\item{impute_coords}{If \code{TRUE} (default), missing \code{lat}/\code{lon} are imputed
from the ICES rectangle midpoint via \code{\link[mapplots:ices.rect]{mapplots::ices.rect()}}.}
}
\value{
A tibble with one row per prey record per predator. Empty and
unidentified stomachs contribute one \code{NA} prey row each.
}
\description{
Reads \code{File_information.csv}, \code{HaulInformation.csv},
\code{PredatorInformation.csv}, and \code{PreyInformation.csv} from \code{path}, joins
them into a single flat tibble, classifies each stomach as \code{"food"},
\code{"empty"}, or \code{"unidentified"}, deduplicates exact-duplicate prey rows,
and optionally imputes missing coordinates from ICES rectangle midpoints.
Also warns if any predator's raw \code{Length}/\code{IndWgt} is wildly inconsistent
with an isometric length-weight curve (\code{W = 0.01 * L^3}), a sign of a
unit error (e.g. length in mm instead of cm), and if any predator's
\code{Number} is \verb{<= 0} (not a valid pooled-sample count) -- neither check
fixes anything, both are raw-data sanity checks -- see
\code{vignette("known-issues", package = "stomachr")} for worked examples of
what these have caught in the live database.
}
